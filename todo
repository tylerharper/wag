html2text for those ugly atom feeds, or a deeper parser for example reddit feed
  - maybe jinja templates/template-tags

a better setup.py that creates dirs for us

code:
  Better layout for args to options
